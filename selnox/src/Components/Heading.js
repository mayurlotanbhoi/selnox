import React from 'react';

const Heading = ({ heading }) => {
        return (
                <div className='heading'>
                        {heading}
                </div>
        );
}

export default Heading;
